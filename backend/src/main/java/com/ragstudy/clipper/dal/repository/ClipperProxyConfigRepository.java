package com.ragstudy.clipper.dal.repository;

import com.ragstudy.clipper.dal.dataobject.ClipperProxyConfigEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClipperProxyConfigRepository extends JpaRepository<ClipperProxyConfigEntity, String> {
}